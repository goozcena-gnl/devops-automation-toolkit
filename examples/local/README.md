# Local examples

Run the foundation health command and generate all report formats:

```bash
devops-toolkit health
devops-toolkit render-sample --format json --output report.json
devops-toolkit render-sample --format markdown --output report.md
devops-toolkit render-sample --format sarif --output report.sarif
```
